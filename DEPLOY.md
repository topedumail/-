# הוראות העלאה ל-Render.com (חינמי)

הבוט מוכן להעלאה ל-Render בקליק אחד. עוקבים אחרי השלבים בסדר.

## דרישות מוקדמות

- ✅ חשבון GitHub עם המאגר הפרטי (כבר יש לך)
- ✅ מפתח OpenAI עם credit (כבר יש לך)
- חשבון Render.com — נירשם בשלב הראשון, חינם

---

## שלב 1 — הרשמה ל-Render

1. כנס/י ל-**https://render.com**
2. לחץ/י על **"Get Started"** ובחר/י **"Sign up with GitHub"**.
3. אשר/י את הגישה ל-GitHub.
4. ב-Render תתבקש/י לאשר גישה למאגר `topedumail/-` — אשר/י.

## שלב 2 — Deploy בקליק אחד

1. ב-Render Dashboard, לחץ/י על **"New +"** למעלה מימין (או משמאל, תלוי בשפה).
2. בחר/י **"Blueprint"**.
3. ברשימה תופיע המאגר `topedumail/-`. לחץ/י **"Connect"**.
4. Render יזהה אוטומטית את הקובץ `render.yaml` ויראה תצוגה מקדימה של ההגדרות.
5. בשדה **OPENAI_API_KEY** — הדבק/י את המפתח שלך (זה שעובד אצלך בקובץ `.env`).
6. לחץ/י **"Apply"** (או "Create Blueprint Instance").

## שלב 3 — המתנה לבילד הראשון

- Render יתחיל לבנות את האפליקציה. **זה לוקח 5–8 דקות** בפעם הראשונה.
- אפשר לעקוב אחרי הלוגים בזמן אמת.
- אם משהו נכשל — תעתיק/י את השגיאה ונתקן ביחד.

## שלב 4 — קבלת הקישור

לאחר שהבילד הצליח, תקבל/י URL כמו:
```
https://education-bot-XXXX.onrender.com
```

זה הקישור שתשלח/י למי שאתה רוצה להראות לו. **HTTPS אוטומטי**.

---

## למה לצפות ב-Free Tier

### יתרונות
- חינם לחלוטין ($0)
- HTTPS אוטומטי
- Auto-deploy בכל push לגיט
- Frankfurt — קרוב לישראל, מהיר

### מגבלות שחשוב לדעת
1. **"Sleep" אחרי 15 דקות חוסר פעילות** — בכניסה הראשונה לאחר חוסר פעילות, השרת לוקח **~30 שניות להתעורר**. תזהיר את הצופים לפני הפגישה: "תכנסו לאתר 5 דקות לפני שמתחילים כדי שיתחמם".
2. **512MB RAM** — מספיק לגמרי לבוט שלך, אבל אסור להעלות יותר מ-500 מסמכים.
3. **משאבי CPU מוגבלים** — תשובה ראשונה אולי תיקח 8–12 שניות במקום 5. סביר.
4. **750 שעות חינם בחודש** — אם רק אפליקציה אחת רצה, זה מספיק לחודש מלא.

---

## איך להתחמם לפני הפגישה

5 דקות לפני שמתחילים:
1. פתח/י את הקישור ב-Render
2. כתוב/י שאלה ראשונה ותקבל/י תשובה (זה "מעיר" את השרת)
3. עכשיו השרת ער למשך 15 הדקות הקרובות — מוכן להדגמה.

---

## עדכונים בעתיד

כל פעם שתעשה/י `git push` ל-main, **Render יבנה אוטומטית גרסה חדשה**. שינויים מקבלים deploy בתוך 3-5 דקות בלי שום פעולה נוספת.

---

## אם תרצה להחליף לשרת שלא נרדם בעתיד

Render משדרגים ב-$7/חודש לתוכנית **Starter**:
- בלי sleep
- 512MB → 512MB (RAM זהה, רק זמינות 24/7)
- אותה ארכיטקטורה, רק לוחצים "Upgrade"

---

## פתרון בעיות נפוצות

**"Build failed"** — תעתיק/י את ההודעה האחרונה מהלוגים ותשלח/י לי.

**"401 Unauthorized" אחרי deploy** — מפתח OpenAI לא הוגדר נכון. ב-Render: Service → Environment → ערוך/י את `OPENAI_API_KEY`.

**הבוט עונה "אין מסמכים במאגר"** — הקבצים בתיקיית `backend/data/knowledge` כן בריפו? בדוק/י ב-GitHub web. אם כן והבעיה נמשכת — אולי build נכשל באמצע. Force redeploy מהדאשבורד.

**זמן תגובה ראשון איטי מאוד** — זה צפוי בפעם הראשונה אחרי 15 דקות שקט. תקרא/י לבוט שאלה אחת "להתחמם".
